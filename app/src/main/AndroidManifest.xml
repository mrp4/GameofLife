<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="scot.provan.gameoflife" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="scot.provan.gameoflife.GameActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="scot.provan.gameoflife.GameSettingsActivity"
            android:label="@string/title_activity_game_settings"
            android:parentActivityName="scot.provan.gameoflife.GameActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="scot.provan.gameoflife.GameActivity" />
        </activity>
    </application>

</manifest>
